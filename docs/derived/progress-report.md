# Progress Report

## Current Phase
- Phase 3A — Public scheduler foundation.

## Completed in this update
- Bootstrapped monorepo workspace with `apps/web` and `packages/contracts`.
- Implemented public scheduler foundation route and direct-link routing.
- Implemented service/date/slot/multi-time/recurring foundation UI flows.
- Implemented visibility filtering to exclude admin-only and internal-use services from public catalog.
- Added tests for visibility filtering, direct-link behavior, and slot generation.

## Validation targets
- `pnpm install`
- `pnpm typecheck`
- `pnpm test`
- `pnpm build`
