# NEXT_AGENT_START_HERE

1. Read `docs/derived/req-index.md` and continue from REQ-008+.
2. Preserve existing visibility filtering boundaries when wiring booking-core APIs.
3. Replace deterministic slot generation with availability engine integration in next phase.
4. Keep direct-link private service behavior while preserving public catalog filtering.
