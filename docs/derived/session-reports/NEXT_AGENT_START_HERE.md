# NEXT_AGENT_START_HERE

## Phase 11A continuation checklist
1. Validate manual mobile QA first on Admin Calendar route (`/admin/calendar`) for 390x844, 360x800, 375x667.
2. Confirm detail panel drawer open/close behavior and sticky Save action remain usable on small screens.
3. Validate public booking (`/booking/demo`) and client self-service (`/client`) for responsive parity and modal usability.
4. Keep route/entity/permission behavior shared with desktop logic (`assets/app.js`).
5. Preserve preview usability while iterating.

## First route for mobile manual review
- `/admin/calendar`
