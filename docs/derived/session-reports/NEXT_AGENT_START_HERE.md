# NEXT_AGENT_START_HERE

## Phase 11A continuation checklist
1. In `/admin/calendar`, block a slot via Block Off Time and confirm it appears in saved appointments.
2. In `/booking/demo`, choose the same date and verify blocked slot is marked unavailable and cannot be confirmed.
3. In `/admin/calendar`, switch Day/Week/Month and verify projection/layout changes meaningfully.
4. Keep responsive parity and shared route/domain logic unchanged.

## First 3 routes for manual re-check
- `/admin/calendar`
- `/booking/demo`
- `/client`

## Shared persistence endpoint
- `/api/appointments`
