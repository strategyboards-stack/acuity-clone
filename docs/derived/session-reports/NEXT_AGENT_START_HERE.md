# NEXT_AGENT_START_HERE

1. Confirm canonical REQ-ID mapping if/when provided; align `docs/derived/req-index.md` as needed.
2. Begin next approved phase only (do not expand admin calendar/payment/integrations logic inside self-service core).
3. For auth hardening phase: move session/users to API+DB, hash passwords, add email token reset workflow.
4. Preserve public vs authenticated client boundary and central appointment aggregate contract.
