# Next Agent Start Here

1. Run repository validation (`pnpm install`, `pnpm typecheck`, `pnpm test`, `pnpm build`) and verify all green.
2. If moving to next phase, preserve contract-first self-service model in `packages/contracts`.
3. Extend Phase 4B action entry routes into full mutation workflows only when explicitly entering next scoped phase.
