# NEXT_AGENT_START_HERE

1. Resolve/confirm registry access for `pnpm` (Corepack currently fails to fetch `pnpm@9.12.0` in this environment).
2. Then run and record:
   - `pnpm install`
   - `pnpm typecheck`
   - `pnpm test`
   - `pnpm build`
3. Keep Phase 6B closed unless bug fixes are needed.
4. For next phase, preserve:
   - appointment-centered aggregate boundaries
   - separation of CRM admin from client/public flows
   - host-shell boundaries for Manage Users/Billing
5. If extending CRM import/export, treat exact format parity as risk-driven follow-up and update risk register accordingly.
